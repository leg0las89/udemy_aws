---
- hosts: localhost
  connection: local
  gather_facts: False
  tasks:
    - name: Import variable
      include_vars: vars/vpc_setup
    - name: Import second vars
      include_vars: vars/vpc_ids